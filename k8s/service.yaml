apiVersion: v1
kind: Service
metadata:
  name: k8s-coach
  namespace: k8s-coach
  labels:
    app: k8s-coach
spec:
  type: ClusterIP
  selector:
    app: k8s-coach
  ports:
    - name: http
      port: 8080
      targetPort: http
