server:
  port: 8080

spring:
  application:
    name: deployable-spring-site

app:
  name: Java 21 Spring Boot Demo
  version: "@project.version@"

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
